# Entorno
.env
.env.local
__pycache__/
*.py[cod]
*$py.class

# Caché local (se refresca desde las APIs)
clarity_cache.json
utm_cache.json
geo_cache.json
tech_cache.json
evgeo_cache.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Vercel
.vercel/
.vercel
.env*

# Datos de la auditoría: el repo es público, así que la data de negocio
# (referencias de pago, montos por merchant) vive en Vercel KV, nunca en git.
audit_cache.json
audit_resolutions.json
tools/ticket_audit/.alerta_estado.json

# Configuración local del editor
.claude/

# Informe de incidente: se queda fuera del repo, que es público
tools/ticket_audit/INFORME_CAUSA_RAIZ.md
